[project]
name = "suritts"
version = "0.1.0"
description = "VieNeu-TTS v3 Turbo CPU streaming demo (FastAPI)"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "numpy",
    "vieneu>=3.6.4",
]

[tool.uv]
package = false
