{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "@dtrensuri/electron-vueuse#build": {
      "dependsOn": ["@dtrensuri/electron-eventa#build"],
      "outputs": ["dist/**"]
    }
  }
}
